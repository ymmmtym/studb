version: '3'
services:
    studb:
        build: .
        image: yumemo/studb
        container_name: studb
        hostname: studb
        tty: true
        volumes:
          - ./app:/app
        ports:
          - "8000:8000"
